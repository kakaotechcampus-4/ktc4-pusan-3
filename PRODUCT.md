# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**1순위 — 1차 배포(10월 3주) 실사용 학부모.** 부산대학교 부설어린이집 보람반·슬기반(5세 40명) 및
3~4세반(60명) 학부모 단톡방이 유일한 채널이다. 앞으로의 모든 설계 결정은 이 사람들의 사용 조건을 기준으로 한다.
멘토·심사 데모는 1순위가 아니다.

**대표 사용자 (기획 §1)** — 김서연 / 34세 / IT 기업 맞벌이. 남편은 출장이 잦고 양가 도움도 어렵다.
5세 민준이의 하원 후부터 다음 날 등원까지가 전부 본인 몫이다.

**사용 상황** — 하루에 여러 번, 자주 밤에, 지친 상태로, 폰 한 손으로 연다. 업무 중 어린이집 메시지가 오는
순간처럼 **다른 일을 하다가 끼어드는 맥락**에서 열리는 경우가 많다.

**하는 일 (job)** — 흩어진 정보(알림장·급식·일정·과거 반응)를 직접 찾아 연결하고, 무엇이 지금 중요한지
판단한 뒤, 다음 행동을 결정하는 일. 이 반복적인 **육아 인지 노동**을 대신 준비받는 것이 목적이다.

**함께 보는 보호자** — 배우자·조부모·시터가 초대 링크로 같은 아이를 함께 볼 수 있다
(`POST /children/{cid}/invites`, 화면 10). 소유자(owner)와 초대받은 보호자의 권한은 구분된다.

## Product Purpose

부모가 따로 정리하지 않아도 AI 가 **Child Memory** 를 만들고, 그 기억을 **근거로** 식사·놀이·교육·건강
네 영역에서 "지금 우리 아이에게 맞는 다음 행동"을 준비해 **확인·선택 몇 번**으로 줄인다.

부모에게 새 습관을 요구하지 않는다 — 이미 배우자에게 하던 말의 **수신처만** 바꾼다.

**성공의 정의** — Agent 가 실제 Child Memory 를 근거로 판단했을 때의 보호자 채택률.

| 지표 | 목표 |
| --- | --- |
| 자체 테스트 10개 통과 수 | 9/10 |
| **근거 없는 개인화 추천 건수** | **0건 (하드 기준)** |
| 제안 → 24시간 내 실제 행동 전환율 | 70% |
| 잘못되거나 오래된 기억을 근거로 쓴 비율 | 0 에 가깝게 |
| 블라인드 비교 선택률 · 재방문율 | **미정** (열린 결정) |

**목표가 아닌 것** — 부모가 육아에 덜 참여하게 만드는 것. 줄이는 것은 아이와 함께 있는 시간 **밖에서**
발생하는 '찾기·기억하기·연결하기·비교하기'다.

## Positioning

> 육아를 가장 많이 아는 AI 가 아니라, **우리 아이를 가장 오래 알아온 AI.**

부족한 건 정보가 아니라 **연결**이다. 범용 AI 는 매번 아이의 나이·성향·과거 반응을 다시 설명해야 하지만,
이 서비스는 그 맥락이 이미 아이 중심으로 쌓여 있다. 이웃 제품이 그대로 베낄 수 없는 지점은 두 가지다.

- **근거를 달고 나가는 추천** — 개인화 추천에는 사용한 `memory_id` 가 반드시 첨부된다. 근거가 없으면
  개인화인 척하지 않고 또래 기준 일반 추천임을 화면에 명시한다.
- **되돌릴 수 없는 것만 사람이 승인한다** — 승인 게이트는 캘린더 쓰기와 건강·알레르기 기록 확정 딱 2곳.
  자동 실행 경로는 코드에 존재하지 않는다.

**쌓이는 것(락인)** — 아이별 Child Memory 와 그 위의 Correction 이력. 떠나면 "우리 아이를 아는 상태"를
처음부터 다시 만들어야 하고, 그 비용은 시간이라 돈으로 해결되지 않는다.

**책임 장벽은 없다** — 진단·발달 평가를 하지 않기로 설계했다. 책임을 지지 않는 쪽을 골랐고, 그 대가로
진입장벽도 없다.

## Operating Context

**입력** — 하단 한 줄 입력창에 말하듯 적는다("오늘 있었던 일, 말하듯 적어주세요"). 형식을 요구하지 않는다.
한 줄이 관찰 N 건으로 분리되어 저장되고, 같은 처리 안에서 그 저장분이 검색에 잡힌다.

**처리 파이프라인 (입력 1건 = run 1건)**

```
① 인식  Supervisor — 안전 사전검사(규칙) + 의도 판정(기록형 / 요청형 / 혼합형)
   ↓    승인 게이트 ①  "이렇게 이해했어요" → 맞아요 / 수정할게요 / 아니에요
② 기록  Memory Agent + Curator — 분리 · 병합 · 집계 · 승격
③ 행동  공통 컨텍스트 강제 주입 → 도메인 Agent 최대 2개가 각자 memory.search
④ 결과  Why this / Why now / 사용한 기록 N건
   ↓    승인 게이트 ②  되돌릴 수 없는 것만 사람 앞으로
⑤ 반영  선택 · 거절 · Correction 을 Memory 로 되돌려 기록
```

진행 상황은 SSE(`GET /runs/{rid}/events`)로 화면에 흐른다. 20초를 넘기면 부분 결과로 전환한다 —
Agent 2개 중 1개만 성공해도 그 화면을 보여주고, **성공과 실패를 한 화면에 섞는다.**

**화면** — 프로토타입 기준 00~10. 00 소개·로그인 / 01 첫 진입(아이 만들기) / 02 이야기 하나 /
03 홈 / 04 진행 / 05~07 제안·승인·평가 / 08 기억·교정 / 09 사진·캘린더 / 10 설정.
`/design-system` 은 내부 문서 화면이다.

**로그인** — 카카오 OAuth. 프론트는 카카오 토큰을 직접 받지 않고, 서버가 인가 코드를 교환한다.
신규 회원은 동의 4건(계정 2 · 아이 2)을 받아야 계정이 만들어지고, **동의는 저장보다 먼저다.**

**모바일 셸** — `apps/mobile` 은 `apps/web` 을 띄우는 Expo WebView 껍데기다. 화면·상태·API 호출을
하나도 갖지 않는다. 따라서 디자인 언어는 네이티브가 아니라 웹이며, 실제 사용 기기는 폰이다.

## Capabilities and Constraints

**만드는 것** — Child Memory 자동 구축(Fact / Observation / Inference 3분류), Curator 의 규칙 기반
병합·집계·승격·감쇠, Supervisor 라우팅, 도메인 Agent 4종(`food` · `activity` · `education` · `health`),
근거(memory_id) 첨부 추천, Correction 4버튼(`confirm` / `once_only` / `outdated` / `wrong`),
보호자 초대·공유, 캘린더 쓰기, 사진 입력.

**안 만드는 것** — 진단·처방·발달 평가, 상품 추천, 대신 발송·예약·결제, 아이와 직접 대화하는 AI,
정확한 레시피, 무제한 수집.

**절대 규칙 (어기면 서비스가 성립하지 않는다 — 정본은 `CLAUDE.md` §2)**

- 알레르기·검진·건강 정보는 LLM 이 생성·추론·수정하지 않는다. 조회에 실패하면 Food Agent 를 아예 실행하지 않는다.
- Health Agent 는 진단하지 않는다. 같은 증상 3회 반복 → 추천 중단 + 병원 확인 권유 + 정리된 기록만.
- 알레르기 필터 · 날짜/나이 계산 · 일정 충돌은 **코드가** 막는다. 모델에게 묻지 않는다.
- 근거 Memory 가 없으면 일반 추천을 내고 **일반 추천임을 화면에 명시**한다. 되물을 때 질문은 최소 1개.
- 한 번의 관찰을 성향으로 확정하지 않는다. 승격은 Curator 의 반복 집계로만.
- 부모의 말은 아이의 Fact 가 아니다(`caregiver_observation`). 6개월 지난 관심 기록은 단독 근거 금지.
- 되돌릴 수 없는 것은 사람이 승인한다. 승인 게이트는 2곳 — **늘리지도 줄이지도 않는다.** 승인 없는 draft 는 24시간 뒤 만료.
- 로그·모델 입력에 원문 대신 `memory_id`. 수집은 이름(별명)·나이·알레르기 여부까지.

**용어** — Observation Memory / Child Memory / Curator / 승격 / 감쇠 / Supervisor / 의도 3형 /
도메인 Agent / Suggestion / 근거(evidence) / Correction / run / 승인 게이트. 정의는 `CLAUDE.md` §5 가 정본이며
**`activity` 로 통일**한다(`play` · `interest` 는 쓰지 않는다).

**기술 제약** — 백엔드와 AI 는 한 서비스(FastAPI)다. 저장이 검색보다 먼저이고 각 Agent 가 `memory.search` 를
직접 부르기 때문이다. 데이터는 PostgreSQL + pgvector 한 곳. 프론트는 Next.js 16 App Router · Tailwind 4 ·
직접 만든 컴포넌트(외부 UI 킷 없음). 아이 스코프의 정본은 URL 이다.

**명시적으로 미정** (화면 작업을 막지는 않는다)

- 개인정보 **보관 범위·기간** (기한 9월 2주)
- **삭제 범위** — Memory · 원문 · Inference · Embedding · 로그 · 공지 원문의 제3자 정보 (기한 9월 2주)
- **외부 LLM 전달 범위** · 식별정보 제거 · 국외 처리 여부 (기한 9월 2~4주)
- 유저 1인당 월 모델 비용, Agent 참조 문서 출처, 블라인드 비교 목표치
- **서비스 이름** — 현재 화면·메타데이터의 "육아기억 / 육아기억 AI" 는 **임시 표기**다

## Brand Commitments

- **이름은 아직 확정되지 않았다.** 현재 표기(`육아기억`, `육아기억 AI`)는 임시이며, 확정 전까지 이름에
  의존하는 그래픽·워드마크·로고 시스템을 만들지 않는다. 저장소·패키지명(`@yukameo/web`)은 브랜드가 아니다.
- **카카오 로그인 버튼의 색·모양·마크는 카카오가 정한 것을 쓴다.** 우리 팔레트가 아니고, 로그인 화면 밖에서
  쓰지 않는다.
- **본문 서체는 손글씨(학교안심 날개 R), 법률 문서는 Pretendard.** 손글씨는 가독성을 조금 내주고
  온기를 사는 선택이고, 아이에 대한 기록을 다루는 제품이라 그 교환이 성립한다고 봤다. 다만
  **약관·개인정보 처리방침·동의 전문에서는 그 교환을 하지 않는다** — 불리한 조항을 놓치지 않고 읽어야
  하는 글이라 `font-doc`(Pretendard) 을 쓰고, 한 화면에서 두 서체를 섞지 않는다.
- **폰트는 라이선스를 먼저 보고 고른다.** 두 서체 모두 **SIL OFL 1.1** 이라 변환(woff2 서브셋)과
  재배포가 허용된다. 그래야 자체 호스팅할 수 있고, 자체 호스팅이어야 앱이 우리 오리진 밖으로 나가지
  않는다. **외부 폰트 CDN 을 쓰지 않는다.** OFL 이 요구하는 저작권 고지는 폰트 폴더의 `LICENSE.txt`
  와 각 woff2 파일 안에 심어 둔다.
- **말투** — 부모를 가르치지 않는다. 단정하지 않는다("~인 것 같아요" · "병원 확인이 필요해 보여요").
  일반 추천을 개인화인 척 부르지 않는다. 취소는 실패가 아니므로 실패 문구를 붙이지 않는다.
- 시각 시스템의 정본은 `docs/web/design-system-v1.md` 와 `apps/web/src/app/globals.css` 다
  (이 파일은 시각 결정을 소유하지 않는다).

## Evidence on Hand

- `docs/overview/service-plan.md` — 페르소나 · 포지셔닝 · 안전 설계 · 성공 지표 · 첫 100명 채널
- `docs/overview/tech-spec.md` — 요구사항 `F-01`~`F-15` · `NF-01`~`NF-09` · 리스크 · 테스트 전략
- `docs/api/api-interface-v1.html` — 화면 01~10 을 그리는 API 28개 계약 (요청·응답·상태 전이·에러 코드)
- `docs/web/design-system-v1.md` · `/design-system` 라우트 — 토큰과 대비비가 실제로 렌더되는 살아 있는 버전
- `docs/assets/prototype.html` — 화면 01~10 standalone 시연본
- `docs/assets/agent-flow.png` — 처리 흐름도(실패 시나리오 포함)
- `apps/web/src/mocks/` — 계약서 기준 MSW 시드 데이터 · 시나리오 스위치(백엔드 없이 화면 작업 가능)
- **문제 근거** — Blind 게시글 3건(서로 다른 사람 · 다른 시기 · 같은 불평)

**없는 것 — 지어내지 않는다**

- 실사용자 발화 로그 · 후기 · 채택률 실측치. 1차 배포(10월 3주) 전까지 존재하지 않는다.
- 실제 아이 정보 · 실제 사용자 발화는 **문서·주석·테스트 픽스처에 붙여넣지 않는다** (저장소가 public).
- 아키텍처 다이어그램 원본 · 발표 PDF · 시연 영상은 Notion 에만 있고 저장소에 없다.
- 파트너십 · 인증 · 의료 자문 · 가격 정책은 존재하지 않는다.

## Product Principles

1. **근거 없이 "우리 아이 맞춤"인 척하지 않는다.** 근거가 없으면 일반 추천이라고 화면에 쓰고, 쌓인 기록
   건수를 그대로 보여준다. 개인화와 일반은 타입으로 갈라 둔다.
2. **되돌릴 수 없는 것만 사람 앞으로.** 승인 게이트 2곳을 늘리면 자동화가 무의미해지고, 줄이면 안전이 깨진다.
3. **틀렸을 때 되돌릴 수 있나 / 100% 맞아야 하나** — 못 되돌리면 사람, 100% 맞아야 하면 코드, 나머지가 LLM.
   새 기능은 이 칸을 먼저 고른다.
4. **부분 성공을 숨기지 않는다.** 20초를 넘기면 부분 결과로 넘어가고, 성공과 실패를 한 화면에 함께 보여준다.
5. **100번째에도 안 피곤한 화면.** 지친 상태로 밤에 여는 도구다. 감탄이 아니라 부담 없음이 기준이고,
   부모에게 새 습관·새 입력 형식·추가 프로필 질문을 요구하지 않는다.

## Accessibility & Inclusion

- **WCAG 2.2 AA 를 지킨다** — 본문 4.5:1 · 비텍스트 3:1. 대비비는 `/design-system` 이 실측 계산해 보여준다.
  "흐린 회색 글씨"를 만들지 않는다 — 기록 건수·날짜는 부모가 실제로 읽는 정보다.
- **색이 단독 신호가 되지 않는다.** 도메인 아이콘은 `aria-hidden` 이고, 의미는 항상 옆의 텍스트 라벨이 진다.
- 외부 UI 라이브러리를 쓰지 않으므로 **접근성 구현은 전부 우리 책임**이다(포커스 트랩 · ESC · 스크롤 락).
  손으로 짜면 반드시 빠뜨리는 것만 예외로 네이티브 `<dialog>` · `react-day-picker` 를 얹었다.
- 한 손 · 저조도 · 중단된 상황에서의 사용을 전제한다. 전 구간 1열 고정 · 최대 폭 560px · safe area 준수.
- 언어는 한국어(`lang="ko"`)다. 국제화 요구사항은 없다.
