.next/
out/
build/
node_modules/
pnpm-lock.yaml
next-env.d.ts

# msw 가 생성한 워커. `pnpm exec msw init public` 로 다시 만든다.
public/mockServiceWorker.js

# 마크다운은 손으로 정렬해 둔다 (표 정렬이 prettier 로 깨진다).
*.md

# pretendard 패키지에서 뽑아 온 폰트 CSS. 손으로 고치지 않는다 (파일 머리말 참고).
src/app/pretendard.css
