# 실행 환경 (local | dev | prod)
APP_ENV=local

# 서비스 이름
APP_NAME=ktc4-pusan-3-api

# 앱: 비동기 드라이버 / Alembic: 동기 드라이버 — 같은 DB, 드라이버만 다름
DATABASE_URL=postgresql+asyncpg://dev:dev@localhost:5432/dailyagent
ALEMBIC_DATABASE_URL=postgresql+psycopg://dev:dev@localhost:5432/dailyagent
